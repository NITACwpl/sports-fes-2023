---
interface Props {
	title: string;
    subTitle?: string;
}

const { title, subTitle } = Astro.props;
---
<h2 class="title text-20">{title}</h2>
{subTitle && <h3 class="subTitle text-12">～{subTitle}～</h3>}